version: "3.6"
services:
  front-end:
    image: ghcr.io/pungpeee/front-end:dev     
    build: .
    dockerfile: ./Dockerfile

networks:
    default:
        external:
            name: loykrathong_network
