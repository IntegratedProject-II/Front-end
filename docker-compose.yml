version: "3.6"
services:
  front-end:
    image: ghcr.io/pungpeee/front-end:main     
    build: .
    dockerfile: ./Dockerfile

networks:
    default:
        external:
            name: loykrathong_network  
